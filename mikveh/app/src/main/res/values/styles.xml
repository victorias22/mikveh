<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Spinner style -->
    <style name="CustomSpinnerTheme">
        <item name="android:gravity">center</item>
        <item name="android:textAlignment">center</item>
    </style>

    <!-- Toolbar style -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <!-- RatingBar1 : inside the mikveh profile style -->
    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/light_gray</item>
        <item name="colorControlActivated">@color/golden_yellow</item>
    </style>

<!--    &lt;!&ndash; RatingBar2 : outside, on the mikveh header style &ndash;&gt;-->
<!--    <style name="customRatingBar" parent="android:style/Widget.Material.RatingBar.Medium">-->
<!--    </style>-->

</resources>